#!/bin/sh

# Install packages
sudo pacman -S linux-hardened-headers nvidia-open-dkms nvidia-utils

# Blacklist nouveau & rebuild initramfs
echo -e "blacklist nouveau\noptions nouveau modeset=0" | sudo tee /etc/modprobe.d/nouveau.conf > /dev/null
sudo mkinitcpio -P
